# Database
POSTGRES_USER=rental
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=rental
DATABASE_URL=postgresql+asyncpg://rental:change_me_strong_password@db:5432/rental

# Security - generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_me_to_long_random_string_at_least_32_chars

# App settings
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
MAGIC_LINK_EXPIRE_DAYS=365
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=10
